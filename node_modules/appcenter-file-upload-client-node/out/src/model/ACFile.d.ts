/// <reference types="node" />
import { ACFusFile } from "./../ac-fus-uploader-types";
export declare class ACFile implements ACFusFile {
    private path;
    constructor(path: string);
    get size(): number;
    get name(): string;
    slice(start: number, end: number): Buffer;
}
